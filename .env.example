GEMINI_API_KEY=
RESEARCH_AGENT_MODEL=gemini-3.5-flash-lite

# Reliability knobs (optional — sensible defaults are used if omitted)
LLM_REQUEST_TIMEOUT_SECONDS=60
LLM_MAX_RETRY_ATTEMPTS=3
LLM_MAX_CONCURRENCY=2
